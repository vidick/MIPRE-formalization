<!-- Area survey written by a Claude Code agent on 2026-09-25 for planning/simplification-2026-09-25.md; read-only, grep-and-read evidence plus the compiler probes it names. Consumer counts are snapshots. -->

# find-simplification survey — `MIPRE/Foundations/*.lean` (top level), `NCPoly/`, `Tsirelson/`

Read-only survey, 2026-09-25. 57 top-level files (22,133 lines) + `NCPoly/` (2 files, 892 lines) +
`Tsirelson/` (8 files, 3,471 lines). Method: `refcounts.json` (the mechanical scan) for counts,
`rg -F` on final components for consumers, statement-normalised duplicate scan across all
non-vendored Lean, Mathlib grep by statement shape, and 5 `lean_run_code` probes (all listed
below with their outcome). Blueprint tags checked against `blueprint_tags.txt`. Nothing under
`MIPRE/` or `blueprint/` was edited.

Compiler probes run (5 of the ~8 budget), all `success: true` unless noted:

| # | what was checked | result |
|---|---|---|
| P1 | `S.pushTo G' f = S.pushQ G' id (fun _ => f)` and `isPCC_pushTo` proved by `isPCC_pushQ` | `rfl`; proof accepted |
| P2 | `S.mergeAnswersByQuestion H f = S.pushQ H id f` and `isPCC_mergeAnswersByQuestion` by `isPCC_pushQ` | `rfl`; proof accepted |
| P3 | `MIPRE.sum_mul_le_sqrt` by `Real.sum_mul_le_sqrt_mul_sqrt`; `sum_prod_id` by `Fintype.sum_prod_type`; `sum_prod_swap` by `Fintype.sum_prod_type_right` | all three accepted (a 4th line in the same probe on `IsHermitian` failed only for a missing `import`, not counted) |
| P4 | `S.pushQMeas g f = (S.P.reindex g (Equiv.refl A)).mergeByQuestion f` and `S.push f = S.P.merge f` | both `rfl` |
| P5 | `S'.mergeAnswers G rA rB = S'.mergeAnswersByQuestion G (fun _ => rA) (fun _ => rB)` and `value_le_mergeAnswers` by the ByQuestion lemma | `rfl`; proof accepted |

## Durable candidates

| # | shape | declarations (path:line) | survivor / replacement | consumers (grepped, then read) | est. net lines | risk | confidence |
|---|---|---|---|---|---|---|---|
| C1 | **Duplication** (verbatim copy, general beside special) | `MIPRE.TensorProductStrategy.value_le_mergeAnswers` `MIPRE/Foundations/GameTransport.lean:255` (61 lines) vs `MIPRE.TensorProductStrategy.value_le_mergeAnswersByQuestion` `MIPRE/Foundations/GameTransportByQuestion.lean:53` (59 lines). `diff` after `rA x`→`rA`: only the signature, one `simp only` lemma name and two `sum_fiberwise` arguments differ. `mergeAnswers` (`GameTransport.lean:248`) is definitionally `mergeAnswersByQuestion (fun _ => rA) (fun _ => rB)` (P5). | Keep the ByQuestion pair; move `ProjectiveMeasurement.mergeByQuestion` (+`mergeByQuestion_M`, `_of_id`), `mergeAnswersByQuestion`, `value_le_mergeAnswersByQuestion` up into `GameTransport.lean` (they need only `merge`, already there) and prove `value_le_mergeAnswers` in one line (P5). Neither name is blueprint-tagged. | `value_le_mergeAnswers`: one consumer, `quantumValue_extendAnswers` `GameTransport.lean:368`. `mergeAnswers`: only that lemma. ByQuestion lemma: `Introspection/TypedQuotientGame.lean:68`, `Background/Introspection/CanonicalDecodedStrategy.lean:56`. | **−55** | low | high |
| C2 | **Duplication** (three verbatim proofs that a fibre sum of a projective measurement is projective) | `MIPRE.ProjectiveMeasurement.merge` `GameTransport.lean:134` (19 lines); `MIPRE.SyncStrategy.push` `PerfectStrategy.lean:255` (18 lines); `MIPRE.SyncStrategy.pushQMeas` `SyncPushQ.lean:44` (19 lines). P4: `S.push f = S.P.merge f` and `S.pushQMeas g f = (S.P.reindex g (Equiv.refl A)).mergeByQuestion f` are both `rfl`. | `merge` survives. `pushQMeas g f := (S.P.reindex g (Equiv.refl A)).mergeByQuestion f` (SyncPushQ already imports GameTransport). `push f := S.P.merge f` needs `PerfectStrategy` to import `GameTransport` (GameTransport imports only `Games`; no cycle — PerfectStrategy's closure is `Games, Distances, Closeness`). `push_apply` stays as `rfl`. | `push`: `OracularSound.lean` (23 sites, as `N.push OAns.pairPart`), `OracularTyped.lean:202` (`pushTo`); `pushQMeas`: only `pushQ`. None tagged. | **−30** (two 15-line proof bodies) | low (one new import edge) | high |
| C3 | **Duplication** (special case beside general; same fact proved twice by different routes) | `MIPRE.SyncStrategy.orthogonal` `PerfectStrategy.lean:189` (33 lines, trace-faithfulness argument, needs `S.d_pos`) vs `MIPRE.ProjectiveMeasurement.mul_eq_zero_of_ne` `GameTransport.lean:100` (34 lines, algebraic, any `Matrix n n ℂ`). Statement of the former is the latter at `P := S.P`. | Keep `mul_eq_zero_of_ne` (general); `orthogonal := S.P.mul_eq_zero_of_ne x hab`. Same import edge as C2 (or move `mul_eq_zero_of_ne` into `Games.lean`/`PVM.lean`). Docstring of `orthogonal` explains the trace route; it can be shortened to a pointer. | `orthogonal`: 248 refs, 23 external (Commutation, PVM, Pasting, Sandwich, Introspection/*, Background/QLD/*) — all through the statement, unaffected. `mul_eq_zero_of_ne`: `GameAdapt.lean`, `GameTransport.lean`. Neither tagged. | **−28** | low | high |
| C4 | **Scaffolding / special cases beside the general case** (three "push a synchronous strategy" constructions; the PCC proof is copied three times) | `MIPRE.SyncStrategy.pushTo` `OracularTyped.lean:199` + `isPCC_pushTo` `:238` (14 lines); `MIPRE.SyncStrategy.mergeAnswersByQuestion` `SyncMergeByQuestion.lean:16` + `isPCC_mergeAnswersByQuestion` `:25` (11 lines); `MIPRE.SyncStrategy.pushQ` `SyncPushQ.lean:66` + `isPCC_pushQ` `:73` (15 lines, the general one; its `hμ` is the weaker `0 < G'.μ → 0 < G.μ`). P1/P2: `pushTo G' f = pushQ G' id (fun _ => f)` and `mergeAnswersByQuestion H f = pushQ H id f` are `rfl`, and both PCC lemmas are one-liners from `isPCC_pushQ`. The three PCC proofs are textually the same `change …; rw [sum_mul_sum, sum_mul_sum, sum_comm]; exact …`. | `pushQ`/`isPCC_pushQ` survive. `isPCC_pushTo` and `isPCC_mergeAnswersByQuestion` become one-liners (P1, P2). Import direction: `OracularTyped` would need `SyncPushQ` (SyncPushQ imports `GameTransport`, `PerfectStrategy` only — no cycle; SyncPushQ's own docstring already says it "generalizes `SyncStrategy.pushTo` of `MIPRE/Foundations/OracularTyped`"), and `SyncMergeByQuestion` would import `SyncPushQ`. `pushTo`, `isPCC_pushTo`, `pushQ`, `isPCC_pushQ` are blueprint-tagged, so the names stay; only proofs change. `mergeAnswersByQuestion` (untagged) could become `abbrev … := S.pushQ H id f`, keeping `perfect_mergeAnswersByQuestion` (its hypothesis shape — support-restricted `hD` — is not the `hZ` form of `value_pushQ_eq_one`, so it is not a corollary; keep). | `isPCC_pushTo`: `OracularTyped.lean:314`, Axioms, blueprint; `isPCC_mergeAnswersByQuestion`: `Background/Introspection/CanonicalComplete.lean:116` (discharges `hμ` by `fun _ _ => rfl`). | **−20** | low–med (import edge into `OracularTyped`, a 453-line file with a wide closure) | high |
| C5 | **Mathlib shadows** (exact) | `MIPRE.sum_mul_le_sqrt` `Sandwich.lean:90` = `Real.sum_mul_le_sqrt_mul_sqrt` (Mathlib `Analysis/Real/Sqrt.lean:500`); `MIPRE.sum_prod_id` `Sandwich.lean:110` = `Fintype.sum_prod_type`; `MIPRE.sum_prod_swap` `Sandwich.lean:114` = `Fintype.sum_prod_type_right`. All three confirmed by P3. Also `MIPRE.CL.map_const_eq_replicate` `OracularSampler.lean:95` = `List.map_const'` (already used under that name in `CL/ProductSamplerProg.lean:22`). | Delete and redirect call sites. None of the four is tagged. | `sum_mul_le_sqrt`: `LowDegreeSandwich.lean:548`, `Pasting.lean:118,225`, `Sandwich.lean:408,442`, `OracularTensor.lean:135`, `Background/QLD/PauliBasis.lean:290` (7 sites); `sum_prod_id`: `Sandwich.lean:116`, `Background/QLD/Combined.lean:469`; `sum_prod_swap`: `Sandwich.lean:329,511,550`, `Background/QLD/Combined.lean:455`; `map_const_eq_replicate`: `OracularSampler.lean:375,396,400`. | **−21** (+~14 one-token edits) | low | high |
| C6 | **Superseded route** (a whole module the Lean never uses) | `MIPRE/Foundations/LowDegreeSandwich.lean` — 793 lines, 29 declarations, every one tagged under blueprint `lem:ar-sandwich-support` (`06_proof_structure.tex:5196`). Its **only importer is `MIPRE/Axioms.lean`**. The blueprint node that `\uses` it, `lem:ar-giant-sandwich` (`:5798`), has **no `\lean{}`**, and `Background/AnswerReduction/SoundPoly.lean:24` says explicitly "This replaces the paper's giant sandwich `Λ`". `planning/answer-reduction.md:364`: "The sandwich lemma … is therefore not consumed by soundness. It stays, as the paper's statement." | Not a deletion (tagged, and the planning note is a recorded decision). Reported as a finding: the AR proof route diverged from the paper and left a leaf. Options: (a) keep, mark the node in the blueprint as paper-route/unused-by-Lean so the graph does not suggest it is load-bearing; (b) retire the module and re-tag the node as "not formalized, replaced by `SoundPoly`". Either way, it is 3.6% of the surveyed area with zero Lean consumers, and its private Minkowski/CS section (`sqrt_sum_weighted_sq_add_le`, `sqrt_wsum_le_add3`, `sum_mul_sum_eq_sum_prod`) duplicates shapes in `Sandwich.lean`/`Closeness.lean`. | Lean: none. Blueprint: `lem:ar-sandwich-support` (29 tags), `\uses` from `lem:ar-giant-sandwich` only. | 0 now; −793 if retired with blueprint repair | high (blueprint/planning decision, not a code change) | high on the facts |
| C7 | **Duplication across files** (same lemma under two/three names) | `MIPRE.CL.toBits_zero'` `OracularSampler.lean:84` = `MIPRE.CL.Detyping.toBits_zero` `CL/DetypingProgQueries.lean:24` (**already in OracularSampler's import closure**); `MIPRE.CL.indicatorBits_empty_eq` `OracularSampler.lean:91` = `MIPRE.CL.TypedSampler.indicatorBits_empty'` `CL/ProductSamplerProg.lean:387` = private `MIPRE.Introspection.indicatorBits_empty` `Introspection/Sampler.lean:28`; `MIPRE.CL.indicatorBits_univ_eq` `OracularSampler.lean:87` = private `MIPRE.Introspection.indicatorBits_univ` `Introspection/Sampler.lean:24`. Also `CL.TypedSampler.toBits_zero_vec` `CL/ProductSamplerProg.lean:382` (same statement, 4-line proof vs 1-line). | One copy each, in the lowest file (`CL/DetypingProgQueries.lean` or `CL/DetypingProgBits.lean`); `OracularSampler` and `Introspection/Sampler` drop theirs (both reach `DetypingProgQueries`); `ProductSamplerProg` does not reach it (closure checked) — would need one import or keep its copy. | `toBits_zero'`: `OracularSampler.lean:375`; `indicatorBits_univ_eq`: `:396`; `indicatorBits_empty_eq`: 1 in-file. Private copies: in-file only. | **−15** | low | high |
| C8 | **Duplication** (same statement, two names, one file) | `MIPRE.SyncStrategy.normalizedTrace_re` `PerfectStrategy.lean:145` (RHS `M.trace.re / S.d`) and `MIPRE.SyncStrategy.normalizedTrace_re_eq_ntr` `PerfectStrategy.lean:224` (RHS `ntr M`, proved by the first + `ntr_fin`). Statement-scan flagged them equal after `ntr_fin` unfolding. | Keep `_eq_ntr`; inline `normalizedTrace_re` (3 refs, all in-file). | in-file only. Neither tagged. | −7 | low | med |
| C9 | **Duplication** (two "uniform sample law" developments) | `MIPRE.SeededGame.dist` `OracularGame.lean:85` + `dist_nonneg` `:89` + `sum_dist` `:93` (14-line proof) + `toGame` `:108` versus `MIPRE.SampledGame.dist` `SampledGame.lean:26` + `dist_eq_card` `:30` + `dist_nonneg` + `sum_dist` + `game` `:64`. `SeededGame.dist x y` is literally the RHS of `SampledGame.dist_eq_card S.LA S.LB x y`; `OracularTyped.lean:157-160` already bridges the two by `change SampledGame.dist …` for `oDist`. | `SeededGame.toGame := SampledGame.game S.LA S.LB S.D` and `dist := SampledGame.dist S.LA S.LB` (OracularGame imports only `Games`; SampledGame's closure is `GameAdapt → GameTransport → Games`, no cycle). `SeededGame.toGame` is tagged (name stays). | `S.dist` unfolded at 12 sites (`OracularGame` 6, `OracularSound` 4, `OracularComplete` 2) — each `rw [dist]` would become `rw [SampledGame.dist_eq_card]`. | −20 | **med** (definitional form of `μ` changes under 144 refs of `toGame`) | med |

## Rejected with evidence

- **`HaltingGameValue.SynchronousGame`/`SyncStrategy` vs `MIPRE.SynchronousGame`/`SyncStrategy`** (`GameDescription.lean`, 154 lines of bridge; `GameData.gameValue_le_one` dead beside `quantumValue_le_one`/`syncValue_le_one`): documented design — `MIPRE/HaltingGameValue.lean` is the self-contained headline statement "from scratch on top of Mathlib only". Rejected; the bridge is the price of that.
- **`MIPRE.GNS`** (`GNS.lean`, 527 lines) vs Mathlib `Analysis/CStarAlgebra/GelfandNaimarkSegal.lean` (2025): Mathlib's needs `NonUnitalCStarAlgebra`; MIPRE's is on a bare `⋆`-algebra with a PSD form (instantiated at the free algebra `NCPoly`, not a C*-algebra). Not a shadow. Its 7 dead lemmas (`ι_smul`, `denseRange_ι`, `bdd_id`, `lift_id`, `lift_eq_of_null`, `lift_add`, `lift_comp`) are a speculative lifting API; `denseRange_ι` and `lift_eq_of_null` are named in the module docstring's inventory — the cluster is "API beyond what `Tsirelson/Algebra` consumes", not a superseded route.
- **`OpBound.lean` hand-rolled `Bnd`/`snorm`/`qform`/`evec`** instead of Mathlib's operator norm: docstring records the reason (instance search on `EuclideanSpace ℂ n →L[ℂ] …` times out). `evec_add`/`evec_smul`/`evec_zero` mirror `WithLp.toLp_*`; too small to matter. Rejected.
- **`Distances.lean`: `povmDistance`, `IsPOVMClose`** and the four closeness Props `IsStateClose`, `IsPOVMStateClose` (`StateDistance.lean:154,171`), `IsXPOVMClose` (`CrossConsistency.lean:76`): each has **zero Lean consumers** (count 2, ext 1 = the blueprint tag only; `StateDistance.lean:17` mentions `povmDistance` in prose). All are `def:distance`-style blueprint definitions. Not deletable under the tag rule; noted so the next blueprint pass knows these are definitions the Lean states but never uses.
- **`SyncTransport.lean` sequel chain** `exists_perfectPCC_syncGame_doubled → syncValue_syncGame_eq_one_doubled → gameValue_toGame_eq_one_doubled`: the first two have only the next link + blueprint as consumers, but all three are tagged; `gameValue_toGame_le_of_valStar_le` (un-doubled) is "kept because it holds and costs nothing" per the docstring and is consumed by `Halting/Reduction.lean:75`, `Halting/Enumerate.lean:184`. Rejected.
- **Named-`Prop` wrappers** `RelabelPAEq`/`RelabelPBEq`/`RelabelStateEq` (`GameTransportProjection.lean:40-65`), `MergeByQuestionPAEq`/`PBEq`/`StateEq` (`GameTransportByQuestion.lean:114-150`): docstrings give a rationale ("avoids comparing concrete game parameters while elaborating dependent registers"); `RelabelPAEq`/`relabel_dA` consumed at `Background/Introspection/ExplicitGame.lean:196,211,214`, `MergeByQuestionP*Eq` at `Introspection/TypedQuotientGame.lean:98,105`. The two `*StateEq` wrappers and `mergeAnswersByQuestion_dimensions` are already on the dead list. Rejected beyond that.
- **`TensorProductStrategy.bddAbove_range_value`/`quantumValue_nonneg`** duplicated as `MIPRE.Repetition.bddAbove_range_value`/`quantumValue_nonneg` in `MIPRE/Background/Repetition/Entangled.lean:143,150` — outside this area (bridge module, not vendored); passed on for whoever holds `Background/Repetition`.
- **`ProjectiveMeasurement.isPVM_at`** (`OracularTensor.lean:183`, 11 refs) duplicated as `MIPRE.LIDT.Simul.isPVM_pm` (`Background/LIDT/Extraction.lean:668`) — survivor is in this area; deletion is in the other area.
- **`Weyl.binEquiv_add` vs `binEquiv_eq_iff`** (`WeylBinary.lean:42,50`): flagged by the normaliser only because it truncates at the first `:`; the statements differ. False positive.
- **`SyncStrategy.value_le_copy`/`copy_value_eq_one`** vs `value_le_pushTo` at `f = id`: `copy` is `⟨d, d_pos, P⟩`, not `push id` (singleton fibre sums), so not definitionally a special case. Rejected.

## Hygiene batch (tiny, correct, batch under one PR)

- `MIPRE.sgn_mul_self_eq_one` `Sign.lean:47` — verbatim restatement of `sgn_mul_self` `:36`; already dead. Also dead there: `sgn_ne_zero`, `inv_sgn`.
- `MIPRE.sum_weighted_sqrt_le` `Sandwich.lean:96` (13 lines, tagged) is `Closeness.sum_mul_sqrt_le` `Closeness.lean:426` at `∑ w = 1` (`√1 = 1`); Sandwich reaches Closeness. Proof → 3 lines; name stays (tagged; 19 refs).
- `MIPRE.SeededGame.oFam_selfAdjoint/…/dFam_projective` `OracularSound.lean:517-532`: six pass-throughs to `(N.push _).selfAdjoint/projective`; 3 refs each, in-file.
- `MIPRE.ValueApprox.isHermitian_realSmul_one_add/_sub` `ValueApprox.lean:35,43`: `Matrix.isHermitian_one.smul … |>.add hM` (Mathlib `IsHermitian.add/.smul/isHermitian_one`, `Hermitian.lean:206,249,314`); in-file only. Not probed (the probe lacked the import).
- `MIPRE.REPred.comp'` `ClassMIPStar.lean:52` (`Partrec.comp`), `MIPRE.Cost.levels_mono` `Compression.lean:322` (`monotone_nat_of_le_succ`), `MIPRE.NCPoly.coeff_mul` `NCPoly/Basic.lean:197` (`MonoidAlgebra.coeff_mul`, dead), `NCPoly.coeff_single_apply` (`Finsupp.single_apply`), `Tsirelson.Coded.mem_words`/`nodup_words` (`List.mem_dedup`/`nodup_dedup`), `validWord_append` (`List.forall_mem_append`): one-token pass-throughs.
- Dead **clusters** worth a single sweep rather than one-by-one: `Tsirelson/CodedPoly.lean:344-396` (`toC_coeffRaw_eq_sum_ite`, `_append`, `_cadd`, `_cneg`, `_cscale`, `_cstar`, plus `toC_coeffRaw` itself: a coefficient calculus for the coded polynomials that `UpperRE` never calls); `NCPoly/Cone.lean:339-391` (`abs_re_add_abs_im_coeff_le_l1`, `l1_sub_le`, `l1_sub_comm`, `l1_list_sum_le`: the `ℓ¹` API beyond what `Certificate.lean`'s 31 uses need); `GameAdapt.lean` (`sum_sum_fibre_eq_sum_card`, `succAt_nonneg`, `failAt_le_one`, `sum_one_sub_value_adapt_le`).
- `MIPRE.SyncStrategy.pushQ_M` `SyncPushQ.lean:67` (dead, `rfl`) and `push_apply` `PerfectStrategy.lean:271` (1 ref): with C2 both become `merge_M` instances.

## Files surveyed / not checked

Surveyed (read header + outline, refcounts, duplicate scan): all 57 top-level `Foundations/*.lean`, `NCPoly/{Basic,Cone}`, `Tsirelson/{Algebra,Certificate,Closed,CodedPoly,Conditional,Positivstellensatz,Separation,UpperRE}`. Read in full or in the relevant sections: Sign, Distances, Closeness (1-130, 180-210, 426-440), OpBound (1-240), Swap, TensorFamily, CommutingOperator, SyncMergeByQuestion, SyncPushQ, SyncTransport, GameTransportProjection, ValueApprox, GameDescription, GameTransport (48-63, 98-166, 205-314, 360-380), GameTransportByQuestion (14-188), PerfectStrategy (140-232, 250-277), OracularTyped (172-260), OracularGame (85-132), SampledGame, OracularSampler (55-100), OracularSound (505-535), LowDegreeSandwich (1-140), Sandwich (60-125), GapCompression (55-90), GNS (header, outline), Correlations (header), Blocks (header), WeylBinary (38-56), NCPoly/Cone (300-499 outline), Tsirelson/CodedPoly (328-350).

Not checked / limits:
- The large `Oracular{Decider,DeciderCost}`, `Compression`, `Pasting`, `Commutation`, `Parseval`, `Linearity`, `Expanded`, `Weyl`, `WeylEPR`, `POVMMix`, `POVMValue`, `CrossConsistency`, `Disagreement`, `Dilation`, `StrategyDilation`, `RegisterReindex`, `Verifier*` files were covered only by the statement-duplicate scan, the pass-through filter and refcounts, not read line by line; no cross-file duplicate surfaced in them beyond what is listed.
- `Tsirelson/` and `NCPoly/` were covered by the scans plus the dead clusters; I did not compare `NCPoly` against Mathlib's `FreeAlgebra`/`MonoidAlgebra` API beyond the `coeff_mul` shadow.
- The `hsInner` Cauchy–Schwarz in `Closeness.lean` is proved by hand for a documented reason (Mathlib's `Matrix.toMatrixInnerProductSpace` is a `def`, not an instance); not re-examined.
- Minkowski lemmas in `LowDegreeSandwich.lean:79-120` (`sqrt_sum_weighted_sq_add_le`) likely follow from `norm_add_le` on a weighted `EuclideanSpace`; not probed since the module is a leaf (C6).
