<!-- Area survey written by a Claude Code agent on 2026-09-25 for planning/simplification-2026-09-25.md; read-only, grep-and-read evidence plus the compiler probes it names. Consumer counts are snapshots. -->

# Simplification survey: `MIPRE/Background/QLD/`, files M–Z

Read-only survey, 2026-09-25. 55 files (basenames M–Z), ~21k lines, 1408 declarations.
Consumer counts are `rg -F` on the final name component across `MIPRE/`, minus the declaration
itself, then read; "bp" = the full name appears in the blueprint `\lean{}` tag set
(`blueprint_tags.txt`), which is a policy consumer. The 25 mechanically-dead declarations
(`dead_MIPRE_Background_QLD.txt`) are not re-reported individually.

## Durable candidates

| # | shape | declarations (path:line) | survivor / replacement | consumers (grepped, then read) | est. net lines | risk | conf. |
|---|---|---|---|---|---|---|---|
| 1 | **Duplication, verbatim**: eight `Question.fmtOk` inversion lemmas declared twice | `MIPRE.QLD.PauliBooleanProgram.eq_val_of_fmtOk`, `eq_apoly_of_fmtOk`, `eq_dpoly_of_fmtOk`, `eq_pauliAns_of_fmtOk`, `eq_bit_of_fmtOk_pairB`, `eq_bitPair_of_fmtOk`, `eq_bit_of_fmtOk_var`, `eq_bitTriple_of_fmtOk` — `MIPRE/Background/QLD/PauliBooleanDispatch.lean:19-53` (`section Format`, all `private`); identical statements and identical `cases a <;> first \| exact ⟨_, rfl⟩ \| simp [Question.fmtOk] at h` proofs at `MIPRE/Background/QLD/Win.lean:321-358` (`MIPRE.QLD.eq_val_of_fmtOk` etc., public, bp=1 each) | Keep one copy. `PauliBooleanDispatch` does **not** import `Win` (checked: not in its 196-module closure) but both import `Game.lean`, where `Question.fmtOk` is defined (`Game.lean:289`). Move the eight public lemmas from `Win.lean` to `Game.lean` (next to `fmtOk`), delete the private copies. | Private copies: used only inside `PauliBooleanDispatch.lean`. Public copies: `Win.lean` items, tagged under `lem:qld-win`. Blueprint tags need no change (names stay `MIPRE.QLD.eq_*_of_fmtOk`). | −34 | low | high |
| 2 | **Mathlib/Foundations shadow, verbatim**: `Q² ≤ Q` for `0 ≤ Q ≤ 1` | `MIPRE.mul_self_le_self_of_le_one` — `MIPRE/Background/QLD/Products.lean:48-58` (namespace `MIPRE`, section Generic) | `MIPRE.mul_self_le_of_le_one` — `MIPRE/Foundations/OpBound.lean:254` — same hypotheses, same `noncomm_ring`/`Commute.mul_nonneg` proof, and `OpBound` is already in `Products.lean`'s import closure | One Lean use: `Products.lean:517`. bp=1 (`MIPRE.mul_self_le_self_of_le_one` is listed under `lem:qld-global-products`), so this is replace-and-redirect-the-tag. | −11 | low | high |
| 3 | **Duplication across trees, verbatim**: swapping the state exchanges the two arguments of `xSqNorm` | `MIPRE.QLD.xSqNorm_swapVec` — `MIPRE/Background/QLD/PaddedLines.lean:113-116`; `MIPRE.Introspection.xSqNorm_swapVec` — `MIPRE/Foundations/Introspection/AdaptivePlayerSwap.lean:31-34`. Same statement (argument names differ), same 1-line proof `rw [xSqNorm_eq_snorm_sq, snorm_swapVec_aOp_sub_bOp, xSqNorm_eq_sq]` | Hoist one copy to `MIPRE/Foundations/CrossConsistency.lean` beside `snorm_swapVec_aOp_sub_bOp` (`:718`) — `xSqNorm_eq_sq` (`:479`) and `xSqNorm_eq_snorm_sq` (`:491`) already live there, and both current files import it (closures checked). | QLD copy: `PaddedLines.lean` ×3, `PaddedValue.lean` ×1; bp=1 (`lem:qld-padded-lines`). Introspection copy: `AdaptivePlayerSwap.lean:204`; bp=0. | −4, one tag redirect | low | high |
| 4 | **Duplication, same file group**: `V_B† V_B = 1` stated twice | `MIPRE.QLD.MirrorSimul.bobSwap_conjTranspose_mul'` — `MIPRE/Background/QLD/SwapItemOne.lean:168-169`, proof `M.toSecond.swapA_conjTranspose_mul`; identical to `MIPRE.QLD.MirrorSimul.bobSwap_conjTranspose_mul` — `MIPRE/Background/QLD/Mirror.lean:415-416` (same statement, same proof), and `SwapItemOne` imports `Mirror` (closure checked) | Delete the primed one; its two uses in `SwapItemOne.lean` take the unprimed name unchanged. | Primed: 2 uses, both in `SwapItemOne.lean`; bp=0 (exact-name check). Unprimed: `Soundness.lean` ×3, bp=1. | −3 | low | high |
| 5 | **Superseded route (cluster)**: the "for the other player" corollaries of the expansion stage are bypassed | `MIPRE.QLD.hatObs_commutation_swap` — `MIPRE/Background/QLD/Swap.lean:126-136`; `MIPRE.QLD.hatPOVM_consistency_swap` — `Swap.lean:138-146` (both on the dead list). The module docstring (`Swap.lean:1-16`) says "the two corollaries at the end are what the combining stage actually wants"; the combining stage instead re-derives them inline: `Combined.lean:316` applies `sum_content_hatComm_le` to `(swapVec ψ)` with `swapVec_unit hψ` / `povmValue_swapped_le hfail` directly. | Either delete the two corollaries and fix the docstring, or make `Combined.lean:310-320` (`sum_content_hatComm_le_B`) consume `hatObs_commutation_swap`. The first is the honest one: the consumer chose the inline form. | 0 Lean consumers, bp=0 for both. | −22 | low | high |
| 6 | **Special case beside the general case, no Lean consumer** (cross-area: `Lines.lean` is in the A–L half, reported because the brief asked whether the one-content `pairs_of_lines` survived) | One-content chain: `MIPRE.QLD.pairs_of_lines` (`Lines.lean:997`, the `kX = kZ = id` instance of `pairs_of_lines_gen`) → `MIPRE.QLD.pairs_of_lines_of_items` (`Lines.lean:1042`) → `MIPRE.QLD.qld_pairs_of_lines` (`Lines.lean:1239-1258`). The padded-lines route uses `pairs_of_lines_gen` → `pairs_of_lines_prod` (`Product.lean`) → `pairs_of_lines_prod_of_items` (`Product.lean`, consumed by `PaddedLines.lean`). | `qld_pairs_of_lines` has **no** Lean consumer (only `Axioms.lean`); `pairs_of_lines_of_items` is consumed only by `qld_pairs_of_lines`; `pairs_of_lines` only by `pairs_of_lines_of_items` (`Lines.lean:1101`). The general chain carries the proof. Both special names are tagged under `lem:qld-pairs-of-lines` (`03_background_results.tex:2905-2907`), whose statement is on the content distribution, so this is a **blueprint decision**: either retarget the node at the product form (and the paper's Claim 17-3 input, `pairs_of_lines_prod_of_items`) or keep the ~40-line instance as the node's witness. Not a deletion candidate under the tag policy. | `qld_pairs_of_lines`: 0 Lean, bp=1. `pairs_of_lines_of_items`: 1 Lean (the above), bp=1. `pairs_of_lines`: 1 Lean, bp=1. `aPres_items`, `sum_content_collProb_aPres` are **live** (PaddedValue). | −40 if retargeted, else 0 | med (blueprint) | high on the counts |
| 7 | **Pass-through with no Lean consumer**: `exists_mirrorSimul` packages `mirrorOfGlobalPairs` as a `Nonempty`, which the assembly then does not use because it "forgets" the physical state (`Soundness.lean:45-47`) | `MIPRE.QLD.exists_mirrorSimul` — `MIPRE/Background/QLD/MirrorExists.lean:186-208` | `mirrorOfGlobalPairs` (`MirrorExists.lean:150`) is what `Soundness.lean:389-392` consumes. Also the only signature in QLD carrying both `hm4 : 4 * m ∣ q` and `hq : 48 * m * d ≤ q`, where `Regime.four_mul_dvd_card_of_regime` (`Regime.lean:66`) makes the first redundant. | 0 Lean consumers (the 8 other mentions in `Soundness`, `QLDError` ×4, `ErrorShape` ×2, `Regime` are all docstrings); bp=1 (`thm:qld` node). Tag policy: keep, or retarget the node's `\lean{}` to `mirrorOfGlobalPairs` and delete; the docstrings would need rewording either way. | −20 | low–med | high |

## Rejected or downgraded, with evidence

- **`Ortho.lean:50-97` `ofCLM_add/sub/smul/mul/one/star/sum`, `toCLM_sum/one/nonneg`** — one-line `map_*` wrappers, each used 1–3× in `Ortho`/`Consistency`. Rejected: the file records the rationale (`Ortho.lean:50-52`: "named rather than used through `map_*` because `rw` cannot match a higher-order pattern against a coercion, and every proof in this file is a rewrite").
- **`Lines.lean` `bijective_shift_gen`, `sum_content_shift_gen`** (thin instances of `bijective_shift`, `sum_shift_gen`) — `planning/qld-campaign.md` §"`lem:qld-pairs-of-lines` with the question distribution as a parameter" records them as deliberately "left as thin instances". Also A–L area.
- **`Products.lean:585-618` `sum_snorm_sq_ordZX_le` / `sum_snorm_sq_ordXZ_le`** — 2-line instances of `sum_snorm_sq_ordComb_le`; both consumed by `PaddedLIDT.lean` (×2 each) and are the blueprint-facing forms of `lem:qld-global-products`. Keep.
- **Three-layer pass-through chain `wTilde_*` (ExactPauli) → `SimulPair.wTildeAt_*` (`MTilde.lean:352-372`) → `MirrorSimul.aliceWTilde_*` / `bobWTilde_*` (`SwapItemOne.lean:248-262`)**, and likewise `swapU_conjTranspose_mul` → `swapA_conjTranspose_mul` → `aliceSwap_/bobSwap_conjTranspose_mul`. Each middle layer is consumed only by the next. Rejected: `SwapItemTwo.lean` docstring ("Three things that had to be said", third bullet) records that `M.mirror.Ea` vs `M.Eb` are definitionally but not reducibly equal, so instance search cannot mix them and each spelling needs its own name. Note however that `wTildeAt_mul_add` (`MTilde.lean:361`) and `wTildeAt_mul_wTildeAt` (`MTilde.lean:367`) have **no** Lean consumer at all (only `Axioms.lean`), bp=1; hygiene at most.
- **`Padded.lean:1195-1345` `avgRestr_le_mul_avgAll` → `avgRestr_avgAll_le` → `avgAll_avgRestr_le` → `avgRestr_prod_le_mul_avgAll` → `avgSub_le_mul_avgAll`** — a ladder, but every rung is consumed inside the file by the next and the top is `lem:qld-padded-lines`' input; no rung is a special case of a later general one. Keep.
- **`SwapUnitary.lean:349-440` `norm_sub_sq_le_of_re_inner_ge`, `re_inner_ge_of_two_close`, `norm_sub_normalize_sq_le`** — checked for Mathlib shape: Mathlib has `norm_sub_sq` but not these composite estimates; the file records the paper's repaired arithmetic they encode. Not shadows.
- **Same-short-name collisions that are not duplicates**: `MIPRE.POVM.compress_aOp` (`PaddedLIDT.lean:72`, POVM level) vs `MIPRE.compress_aOp` (`Foundations/Sandwich.lean:776`, matrix level; the former is proved from the latter); `MIPRE.QLD.liftOp` (`PaddedLIDT.lean:307`) vs `MIPRE.QLD.Honest.liftOp` (`Introspection/HonestPauliMeasurements.lean:28`, different registers); `PauliBranchProgram.basisIndex : Bas → ℕ` vs `PauliCL.basisIndex : Bas ≃ Fin 2` (private, `CLBinary.lean`); `isPVM_aliceTau` / `isPVM_bobTau` (`SwapItemTwo.lean:365-369`) have the same proof but different index types.
- **`PaddedValue.lean` L/R pairs** (`lineTermL/R`, `padGL/R`, `sum_bornProb_lineEvalFam_ptFam` / `..._ptFam_lineEvalFam`, `one_sub_agreeSum_lineEval_pt_le` / `..._pt_lineEval_le`, `padW_line_point_le` / `padW_point_line_le`) and **`PaddedStrategy.lean:545-625`** (`accepts_lineQ_point` / `accepts_point_lineQ`, `condFail_lineQ_point_le` / `condFail_point_lineQ_le`) — genuine mirror re-proofs (each ~10 lines), but no `CL.accepts` symmetry lemma exists to transport them (only `clGame_μ_symm`, `LIDT/Adapter/Weights.lean:711`), so writing one would not save lines. Downgraded to hygiene.
- **Multilinear/NonMultilinear**: `sum_sub_le_of_eq_on` (`NonMultilinear.lean`) takes the "agreement on the interpolants" as a hypothesis; its single call site (`Multilinear.lean:537`) discharges it with `dotF_cubeData_indVec` (`Multilinear.lean:283`). Folding would specialise a generic lemma; rejected. But the `NonMultilinear.lean` docstring still calls that fact "an outstanding ingredient" — stale (hygiene).

## Hygiene batch (tiny, correct, batch under one PR)

- `MIPRE.QLD.two_le_card_field` (`Soundness.lean:157`) is literally `Fintype.one_lt_card`; 1 use. Tagged (`thm:qld` list) — inline and drop from the tag.
- `MIPRE.QLD.card_Content_pos` (`Win.lean:68`) and `MIPRE.QLD.card_content_pos` (`Lines.lean:238`, on the dead list) are the same statement with the same proof (`Fintype.card_pos_iff.mpr ⟨⟨0,0,0,0,0,0⟩⟩`); both are `Fintype.card_pos` given the `Nonempty (Content F m)` instance at `Lines.lean:236`.
- `mul_card_div` (`Padded.lean:61`) is `Nat.mul_div_cancel' hm`; 2 uses.
- `errSmall_qldDelta := errSmall_deltaS`, `errSmall_qldHlt := errSmall_itemOneEta`, `errSmall_qldEta := errSmall_swapItemOne` (`QLDError.lean:260-265`): pure renames, 1–2 uses each, all tagged. Keep or fold; no lines to gain beyond 3.
- Template lemmas with zero consumers, untagged: `PauliBinaryProgram.program_runs` (`PauliBinaryPrograms.lean:84`), `PauliBooleanProgram.program_runs` (`PauliBooleanPrograms.lean:143`), `PauliFullAnswerProgram.program_runs` (`PauliFullAnswerPrograms.lean:74`), `PauliAnswerProgram.parser_runs` (`PauliAnswerPrograms.lean:117`, on the dead list) — each is `X.computes input`. `SamplerProgram.query_runs` (`SamplerQueryProgram.lean:101`) is the same shape but tagged. Together with the dead `program_rejects_left/right`, `parser_valid`, `program_valid`, `registerVector_binEquiv`, `program_output_length`, `route_adj`, `route_var_con`, `dispatchProg_apply`, `route_default_orientation`, 12 of the 25 dead entries sit in this 20-file, 3010-line `Pauli*Programs` cluster: the cluster is live (chain `PauliAnswerPrograms → … → PauliBooleanRaw → PauliBinaryInterface → Introspection/DecisionKernelInput`), but its "runs/valid/rejects" API surface was speculative.
- `PauliArithmeticProgram.answerRows E h` (`PauliArithmeticPrograms.lean:45`) is definitionally `PauliAnswerProgram.answerRows E (.pauliAns h)` (`PauliAnswerCoding.lean:19`); `PauliArithmeticPrograms` does not import `PauliAnswerCoding`, so the two coexist with no bridging lemma.
- Generic lemmas living in QLD that belong in `Foundations/`: `MIPRE.snorm_sq_add_le` (`Products.lean:72`; the same inequality is re-proved inline at `Foundations/OracularTensor.lean:122` and `Foundations/Introspection/PVMStateTransfer.lean:37,51`), `MIPRE.bornProb_mono_left` (`Products.lean:85`, the twin of `bornProb_mono_right`), `MIPRE.stateSqNorm_extVec2_aOp` (`Products.lean:132`, the Alice twin of `Foundations/Sandwich.lean` `normSq_stateVecB_extVec2_aOp`), `swapVec_sum` and `sum_fibre_smul` (`SelfCons.lean:49,68`; `swapVec_sub` is at `CrossConsistency.lean:707`). Hoisting is net −0 to −6 lines (the two inline re-proofs) but fixes placement.
- `condFail_aline_dline_le` / `condFail_dline_aline_le` (`PaddedStrategy.lean:663-690`) and `padW_aline_dline_le` / `padW_dline_aline_le` (`PaddedValue.lean:814-831`) are identical up to `.aline`/`.dline`; one lemma each with `(ty ty' : CL.Ty) (h : ty ≠ ty') (hty : ty ≠ .point) (hty' : ty' ≠ .point)` saves ~25 lines.
- `exists_mirrorSimul` (`MirrorExists.lean:192`) carries `hm4` beside `hq` although `Regime.four_mul_dvd_card_of_regime` derives it; `Regime.lean` is imported only by `Soundness.lean`, so the derivation sits after the only signature that could use it.
- `SelfCons.lean` has no module docstring (every other file in the half has one). `NonMultilinear.lean` docstring's "What is not here" paragraph is stale (see above).

## Files surveyed

All 55 M–Z basenames: module docstrings read for every non-`Pauli*Program` file and for the
program cluster; declaration outlines extracted for all; full text read for `Products.lean` (generic
sections), `PaddedStrategy.lean:540-700`, `PaddedValue.lean:130-190, 455-490, 770-840`,
`Padded.lean:1195-1345`, `Separate.lean:36-200`, `Swap.lean:95-150`, `SwapItemOne.lean:160-262`,
`SwapItemTwo.lean:345-370`, `Mirror.lean:398-425`, `MirrorExists.lean:40-230`, `MTilde.lean:352-372`,
`Multilinear.lean:500-557`, `QLDError.lean:100-275`, `Regime.lean`, `Ortho.lean:50-100`,
`Soundness.lean:100-175`, `Win.lean:315-360`, `PauliBooleanDispatch.lean:14-60`, and the
`Pauli*` program file headers. `planning/qld-campaign.md` read in full for recorded rationales.

## Not checked

- No `lean-lsp` compiler probes were spent: every durable candidate is a textual identity (same
  statement, same proof) or a verified import-closure fact, and `exact?` would add nothing.
- The A–L files (`Lines.lean`, `Game.lean`, `Combined.lean`, `Consistency.lean`) were read only where
  an M–Z candidate led there; row 6 is reported for the sibling's area.
- `ChainAssembly`/`Chain` (A–L, 3.1k lines) were not examined for the `SimulPair` fields' consumers.
