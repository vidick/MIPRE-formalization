<!-- Area survey written by a Claude Code agent on 2026-09-25 for planning/simplification-2026-09-25.md; read-only, grep-and-read evidence plus the compiler probes it names. Consumer counts are snapshots. -->

# Simplification survey: `MIPRE/Foundations/Cost/` and `MIPRE/Foundations/SAT/`

Read-only survey, 2026-09-25. Consumer counts are `rg -F` on the final name component over
all of `MIPRE/` (Background included) and `blueprint/src`, minus the declaration itself; every
count below was then confirmed by reading the hits. Blueprint tags were checked against
`blueprint_tags.txt`. Five Mathlib-shadow probes were run with `exact?` (see "rejected").

## Durable candidates

| # | shape | declarations (path:line) | survivor / replacement | consumers (grepped, then read) | est. net lines | risk | conf. |
|---|---|---|---|---|---|---|---|
| 1 | dead file / superseded route | `MIPRE/Foundations/Cost/ProgData.lean` (264 lines): `MIPRE.Cost.ofNat_of_toNat?` :41, `toData_of_ofData` :49, `progOk` :76, `ofNat_inj` :90, `progOk_spec` :94, `primrec_progOk` :204, `progNorm` :238, `progNorm_eq` :240, `primrec_progNorm` :258 | delete the file; drop `import MIPRE.Foundations.Cost.ProgData` from `MIPRE/Foundations/Halting/Instantiation.lean:9` (Instantiation uses none of its names; `Cost.Codable` is already reached through its other imports); `lake exe mk_all` | 0 Lean consumers. Only importer is `Halting/Instantiation.lean`, which references no ProgData name. The two outside hits for `progOk`/`progNorm` are docstring prose in `Halting/Wrapper.lean:37-38` saying the route is gone. The file's own docstring (ProgData.lean:14-20) records that its consumer ("obligation O2 tabulation") no longer decides program-hood. Not tagged, not guarded. | −265 | low | high |
| 2 | dead scaffolding / superseded route | `MIPRE.Cost.evalFuel` `MIPRE/Foundations/Cost/Basic.lean:425`, `MIPRE.Cost.evalFuel_sound` :451 (block :423-511), inventory line :107 | delete; the computable evaluator the project actually uses is `Machine.runForD` (`Cost/BoundedEval.lean`), which also has the completeness direction `evalFuel` lacks | 0 Lean consumers (only in-file, 27 self-hits). `planning/compression-track.md:54,148,256,377` mention it as a K1 deliverable / "#eval pins", but no `#eval`, test or scratch pin exists in the repo. `Cost/Partrec.lean`'s partial-recursiveness goes through `stepData`, not `evalFuel`. | −90 | low | high |
| 3 | duplication (two developments of the binary successor) | **Survivor (this area):** `MIPRE.Cost.incBits` `Cost/Binary.lean:89`, `incBits_bits` :107, `Prog.incBitsProg` :235, `incBitsProg_runs` :241, `PolyTimeFun.inc` :297. **Duplicate (sibling area):** `MIPRE.Cost.Data.inc` `Halting/Arith.lean:42` (same equations as `incBits`), `bitsVal_inc` :47 (= `SAT/FmlLib.lean:96 bitsVal_incBits`), `inc_ne_nil` :57, `inc_canon` :62, `inc_bits` :88 (= `incBits_bits`), `incAcc`/`incAcc_spec`/`incAcc_length` :99-123, and the whole program block `Prog.incBody`/`incProg`/`incBody_nil`/`incBody_false`/`incBody_true`/`incLoop_runs`/`incProg_runs`/`incProg_runs_nat` :210-357 (same program shape `let_ (cons (var 0) nil) (let_ (loop body) revOntoProg)`) | keep `Cost/Binary.lean`; in `Halting/Compressor.lean` replace `callVar 2 incProg` / `callVar 4 incProg` (:76, :80) and `incProg_runs_nat` (:143) by `incBitsProg` / `incBitsProg_runs` (or `PolyTimeFun.inc.computes`), then delete Arith.lean:39-123 and :210-357; move `bitsVal_incBits` from `SAT/FmlLib.lean:96` to `Cost/Binary.lean` beside `incBits` | `Data.inc` family: 0 consumers outside Arith.lean (`inc_canon`, `incAcc*` have none even inside Halting beyond `inc_bits`'s proof). `incProg`: consumed only by `Halting/Compressor.lean` (:76, :80, :100-101, :143, :148, :160). `PolyTimeFun.inc`: ~10 consumers across Cost, SAT, Introspection, TM. Neither copy is tagged. | −220 | med (Compressor's cost arithmetic is stated against `(Nat.size n + 2) * (8 esize n + 8 size n + 60)`; the survivor gives `(l.length + 2) * (3 esize l + 32)`, smaller, so the `omega`/`nlinarith` steps should still close, but this is where the migration can bite) | med-high |
| 4 | pass-through re-export (indirection) | `MIPRE.SAT.Circuit.zeroBits` `SAT/GateFieldEval.lean:22`, `oneBits` :24, `length_zeroBits` :25, `length_oneBits` :27, `evalBits_zeroBits` :29, `evalBits_oneBits` :31, `zeroBitsProg` :33, `oneBitsProg` :34, `zeroBitsProg_apply` :35, `oneBitsProg_apply` :36 | `MIPRE.LowDegree.BinaryPolynomial.{zeroBits, oneBits, length_zeroBits, length_oneBits, evalBits_zeroBits, evalBits_oneBits, zeroBitsProg, oneBitsProg, zeroBitsProg_apply, oneBitsProg_apply}` (`LowDegree/BinaryConstants.lean:14-45`), which GateFieldEval already imports | The only files that see the `Circuit.*` copies are `SAT/CircuitFieldEval.lean` and `SAT/CircuitFieldCorrect.lean` (namespace `MIPRE.SAT.Circuit`), and both already `open LowDegree.BinaryPolynomial`; `SAT/PcpFieldTests.lean` and `SAT/TraceGram.lean` already use the `BinaryPolynomial.` names. No `Circuit.zeroBits`-qualified reference exists anywhere. The comment at :20 ("retain for downstream compiled consumers") names no consumer. | −17 | low | high |
| 5 | dead cluster the docstring advertises | `MIPRE.SAT.Circuit.tseitin_sat_of_eval` `SAT/Tseitin.lean:183`, `MIPRE.SAT.Circuit.tseitin_sat_iff` :214 (block :181-222) | delete; the tableau (`MIPRE/TM/CookLevin/`) consumes `tseitin_sat_of_values` and `eval_of_tseitin_sat` (2 hits), which are the two halves in the form S1 needs; update the module docstring (:20-22) which still names `tseitin_sat_iff` as the main result | 0 consumers (each name's only other hit is the other's proof). No `\lean{}` tag or guard names any Tseitin declaration (the blueprint's `lem:tseitin` has no `\lean{}` at all — worth tagging `tseitin_sat_of_values`/`eval_of_tseitin_sat` while there). | −42 | low | high |
| 6 | special case kept beside the general case (+ tag redirect) | `MIPRE.SAT.binFieldGalois` `SAT/AdmissibleField.lean:46`, `MIPRE.SAT.nonempty_binField` :67, private `bitOf`/`ite_bitOf` :37-41, `import Mathlib.FieldTheory.Finite.GaloisField` | `MIPRE.SAT.shoupBinField k hk : BinField k` (`SAT/QuotientField.lean:155`) exists for every `k ≥ 1`, so `nonempty_binField k hk := ⟨shoupBinField k (Nat.pos_of_ne_zero hk)⟩` is a one-liner; the file shrinks to that (or moves into QuotientField.lean) and the GaloisField import goes | `binFieldGalois`: 2 consumers, both policy — `blueprint/src/content/03_background_results.tex:6739` (`lem:admissible-field-exists`) and `MIPRE/Axioms.lean:969-970`. `nonempty_binField`: same two. Nothing in the Lean pipeline uses either (the PCP uses `shoupAdmissibleField`, `TM/CookLevin/ClassicalPcp.lean:24,96`). The file's docstring rationale ("nothing should be blocked on an existence question") is recorded but is now discharged by the Shoup construction. | −55 | low | med (tag redirect + guard edit; low priority per rules) |
| 7 | duplication across trees, survivor in this area | (a) `MIPRE.Cost.esize_nat_le` `Cost/Encoding.lean:147` ↔ `MIPRE.TM.CookLevin.Desc.esize_nat_le` `TM/CookLevin/Assemble.lean:36` (identical statement); (b) `Nat.bits_two_pow` `Cost/Unary.lean:86` ↔ `Halting/CompressorProgram.lean:35 bits_two_pow` ↔ `TM/CookLevin/LayoutProg.lean:121 bits_two_pow` (identical); (c) `MIPRE.SAT.vecBits_eq_ofFn` `SAT/FieldVectors.lean:16`, `getD_vecBits` :26 ↔ `Background/AnswerReduction/StageCorrect.lean:99 vecBits_eq_ofFn`, `DecideSpec.lean:288 getD_vecBits` (same statements at `E := shoupBinField k hk`) | keep the Cost/SAT copies, delete the others (the duplicates all sit in files that already import the survivor's module transitively; (c) needs `import MIPRE.Foundations.SAT.FieldVectors` in the two AnswerReduction files, or the two lemmas move to `SAT/Pcp.lean` next to `vecBits`) | (a) `Desc.esize_nat_le`: 4 uses in `TM/CookLevin/DecoupledProg.lean:362-365`, all resolvable to the Cost one by name; (b) CompressorProgram's copy: 1 use (:51); LayoutProg's: 2 uses (:124 recursive, :148); (c) 1-3 uses each in their files | −45 | low | high |
| 8 | hypothesis-strength ladder around the universal-machine specs (context item; not this area) | `Turing.exists_universalCode`, `Turing.exists_boundedUniversalCode` `MIPRE/TM/Universal/Spec.lean` (the two `sorry`s) | — | 0 Lean importers of `MIPRE.TM.Universal.Spec` besides `MIPRE.lean`; no `\uses{lem:universal-machine}`/`\uses{lem:bounded-universal-machine}` anywhere in `blueprint/src` (grep -U); kept alive only by their own `\lean{}` tags at `04_computability.tex:86,104` and the prose at :219. `planning/tm-infrastructure.md:229-236` records them as deliberately retained "paper-literal machine statements" and `CONTRIBUTING.md` as the standing sorry exception — a recorded rationale, so flagged, not proposed. If the owner wants them gone, the deletion is the file plus the two blueprint nodes (no dependency edge breaks). | (−60 if taken) | — | n/a |

## Rejected with evidence

- **Mathlib shadows probed with `exact?` (one `lean_run_code`, Mathlib imports `Nat.Bits`, `Nat.Size`, `Polynomial.Eval.Defs`, `Primrec`)** — none closed: `MIPRE.Cost.polynomial_eval_mono` (`PolyTime.lean:36`), `MIPRE.Cost.Nat.foldr_bit_bits` (`Encoding.lean:134`), `MIPRE.Cost.two_pow_size_le` (`Growth.lean:26`), `n ≤ Nat.nextPowerOfTwo n` (so `MIPRE.SAT.ceilPower` `PowerPadding.lean:22` cannot be swapped for core's `Nat.nextPowerOfTwo` without also writing its order lemmas; `ceilPower_le_twice`/`ceilPowerProg` are tagged anyway), `Primrec₂ (· ^ ·)` (so `Codable.lean:372 primrec_nat_pow` stays, as its docstring claims). Three loogle queries returned nothing.
- **`Cost/Universal.lean:1411-1552` `stepPoly/interpBodyPoly/sigmaPoly/univPoly/sizePoly/addPoly/triplePoly/repPoly/subPoly/stepCostPoly/clockBodyPoly/thetaPoly/univTPoly` + `_eval` ladder** — each `_eval` used exactly once by the next, but this is the "time bound is data" packaging `PolyTime.lean`'s docstring requires; no deletion is enabled by merging them.
- **Per-case `*_wellScoped` / `*_runs` families in `Interpreter.lean`, `Universal.lean`, `Loops.lean`, `Numeric.lean`, `TreeBits.lean`, `Unary.lean`, `Semidecide.lean`, `Flatten.lean`, `GateProg.lean`** (the bulk of the 384 "referenced once" entries) — proof structure consumed once by the next lemma; collapsing them adds no deletion, only longer proofs.
- **`MIPRE.Cost.whileProg`/`seqProg` (`While.lean`)** — `seqProg p q := .let_ p q` is a thin wrapper, but live: `Pipeline/UnaryArith.lean`, `Pipeline/PowDomRun.lean`, `Background/AnswerReduction/Params.lean` (10+ uses).
- **`MIPRE.SAT.instSizedEncodingListGate` (`Circuit.lean:85`) vs the generic `instSizedEncodingList` (`Fold.lean`)** — definitionally equal, but `Circuit.lean` imports only `Cost.Encoding` and precedes `Fold.lean`; the specific instance is what keeps that import light.
- **`SAT/Arithmetization.lean`** (imported only by `MIPRE/Axioms.lean`; formula arithmetization while the pipeline uses the circuit version) — tagged and guarded (`lem:formula-arithmetization`), so a blueprint-only leaf by policy; only `degreeOf_arith_le_of_occurrences` :77 is dead (already on the mechanical list).
- **`Cost/SizeProgram.lean encodedSizeU`** (2 consumers) — its `timeBound` `(X+1)*(5X+20)+X+6` is literally `Universal.sizePoly X`, but Universal.lean is far heavier than SizeProgram.lean; no sensible merge.
- **`Cost/Iterates.lean recordIteratesProg`**, **`Cost/Threshold.lean threshold`**, **`Cost/Clocked.lean haltsWithin`**, **`Cost/Succinct.lean bitQueryProg`** — single- or two-consumer definitions, all tagged or consumed by tagged results; correct scaffolding.
- **`ArrayProg.getD` vs `Reader.nthD`** — not duplicates: binary runtime index vs Lean-level constant index.

## Hygiene batch (tiny, correct, batch under one PR)

- `MIPRE.Cost.PolyTimeFun.ap₂'` + `ap₂'_apply` (`Cost/Reader.lean:50-57`): dead pair (the def's only reference is its own simp lemma). −8.
- `esize_bool_le` declared `private` three times: `SAT/ArrayProg.lean:27`, `SAT/PcpFormat.lean:27`, `Cost/BinaryCompare.lean:48`; one public lemma next to `size_ofBool` (`Cost/Encoding.lean:75`). −4.
- `SAT/PowerPadding.lean:44 unary_eq_length` (private) is `(unary_length u).symm` (`Cost/Fold.lean:537`); `:49 esize_unary_list` (private) duplicates the public `MIPRE.LowDegree.esize_unary_list` (`LowDegree/UnaryDegreeArithmetic.lean:166`, 3 outside consumers) — the public one belongs in `Cost/Fold.lean` beside `esize_unary`. −10.
- `MIPRE.SAT.Fml.refCount` (`SAT/Formula.lean:333`) is `Circuit.fanout` unfolded (`fanout_eq_refCount` :344 is `rfl`, and dead); state `refCount_flattenAt_eq_zero` on `fanout` of the circuit and drop `refCount`. −8.
- `lit_true_iff` private twice (`SAT/Padding.lean:71`, `TM/CookLevin/PcpClauses.lean:94`); one lemma in `SAT/Cnf.lean`. −4.
- `Cost/BinaryArithmetic.lean:79 split_bits_of_ne_zero` (private) re-derives `exists_split_of_any` (`Cost/Numeric.lean:96`) for `n.bits`; `Cost/BitQuery.lean:61 decProg_runs'` is the same derivation at the `Runs` level. Fine to keep, but the value-level facts (`decBits`, `bitsVal_decBits`) would sit better in `Numeric.lean` beside `decProg` than in the contributor file.
- `SAT/FieldCoordinates.lean:12-22` opens `namespace MIPRE.LowDegree.BinaryLinear` to prove `vectorBits_vectorValue`; it belongs in `LowDegree/BinaryLinear*.lean`.
- `Cost/FiniteChoice.lean` imports `MIPRE.Foundations.CL.DetypingProgFinite` — the only `Cost/` file that depends on `CL/` (for `finiteFunction`); either `finiteFunction` moves down to Cost or `FiniteChoice` moves up to CL. No line change, layering only.
- `Cost/Basic.lean:107` inventory line for `evalFuel` goes with candidate 2; `SAT/Tseitin.lean:20-22` docstring with candidate 5; `SAT/GateFieldEval.lean:20` comment with candidate 4.
- `Halting/Arith.lean` also carries `inc_canon`, `incAcc`, `incAcc_spec`, `incAcc_length`, `hornerCoeffs`, `size_encode_append`, `size_encode_reverse` with zero consumers outside the file (sibling area; goes with candidate 3).

## Files surveyed and what could not be checked

- All 36 `Cost/` and 46 `SAT/` files: module docstrings read in full; declaration index (1656 declarations) with reference counts built (`decl_index.tsv`); same-name-across-files scan over the non-vendored tree (`dups.txt`); bodies read for Basic, Binary, BinaryArithmetic, BinaryCompare, Clocked, Fold, Growth, Interpreter (tail), Kleene, Partrec, ProgData, Reader, SizeProgram, Threshold, Toolkit, Universal (tail), While, and for AdmissibleField, ArrayProg, Circuit, Cnf, FieldVectors, Flatten (parts), FmlLib (parts), Formula (parts), GateFieldEval, Padding, Pcp, PowerPadding, Succinct, Tseitin. The Shoup/normal-basis chain (`QuotientField` … `EffectiveNormalBasis`, 8 files) and `PcpBlocks`/`PcpAlgebra` were only skimmed at header level: they are single-purpose ladders feeding tagged results and showed no cross-file duplication in the scan.
- Not compiled: candidates 1-7 rest on grep + reading, not on a build (read-only session); candidate 1 and 2 should be confirmed by deleting and running `lake build MIPRE.Foundations.Halting.Instantiation` / `MIPRE.Foundations.Cost.Basic`, candidate 3 by rebuilding `Halting/Compressor.lean`.
- `@[simp]`-only usage (a lemma applied by `simp` without being named) cannot be seen by grep; the `_apply` lemmas in the "own=1" list were therefore not reported except where the underlying definition is itself unreferenced (`ap₂'`).
- Planning documents read: `CLAUDE.md`, `planning/tm-infrastructure.md` (headings, gate decision, D13), `planning/succinct-cook-levin.md` (pieces and status).
