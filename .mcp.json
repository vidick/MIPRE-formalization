{
  "mcpServers": {
    "lean-lsp": {
      "type": "stdio",
      "command": "uvx",
      "args": ["lean-lsp-mcp"],
      "env": {
        "LEAN_PROJECT_PATH": "${LEAN_PROJECT_PATH:-/home/user/MIPRE-formalization}"
      }
    }
  }
}
